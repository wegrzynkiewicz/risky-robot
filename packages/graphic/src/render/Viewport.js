export default class Viewport {

    constructor({name, x, y, width, height}) {
        this.name = name;
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }

    render(system, context) {
        // TODO: draw viewport
    }
}
