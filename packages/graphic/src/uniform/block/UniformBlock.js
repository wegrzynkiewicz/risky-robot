export default class UniformBlock {

    constructor({name, blockIndex, bindingIndex}) {
        this.name = name;
        this.blockIndex = blockIndex;
        this.bindingIndex = bindingIndex;
    }

    static create() {

    }
}
